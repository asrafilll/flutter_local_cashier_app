import 'package:flutter/material.dart';

class AdminChartPage extends StatelessWidget {
  const AdminChartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AdminChartPage'),
      ),
    );
  }
}
